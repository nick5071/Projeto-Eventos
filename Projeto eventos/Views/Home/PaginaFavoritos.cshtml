@model IEnumerable<Projeto_eventos.Models.Evento>
@{
    ViewData["Title"] = "Meus Favoritos";
}

<script src="https://cdn.jsdelivr.net/npm/particles.js@2.0.0/particles.min.js"></script>

<div class="min-h-screen font-sans bg-gray-900 text-gray-100 relative overflow-hidden">

    <div id="particles-js" class="fixed inset-0 z-0"></div>

    <header id="navbar" class="fixed top-0 w-full z-50 backdrop-blur-md bg-gray-900/70 border-b border-gray-700 transition-all duration-300">
        <div class="container mx-auto px-6 py-4 flex justify-between items-center relative z-50">

            <h1 class="text-3xl font-extrabold tracking-tight bg-gradient-to-r from-purple-500 via-pink-500 to-indigo-500 text-transparent bg-clip-text">
                EventX
            </h1>

            <nav class="hidden md:flex gap-8 font-semibold text-gray-200">
                <a asp-controller="Home" asp-action="Index" class="hover:text-purple-400 transition">Início</a>
                <a asp-controller="Home" asp-action="TodosEventos" class="hover:text-purple-400 transition">Eventos</a>
                <a asp-controller="Home" asp-action="PaginaFavoritos" class="text-pink-400 font-semibold">Favoritos</a>
                <a class="hover:text-purple-400 transition">Contato</a>
            </nav>

            <div class="flex items-center gap-4">
                @if (User.Identity.IsAuthenticated)
                {
                    <div class="relative">
                        <button id="userDropdownButton" class="flex items-center gap-2 px-4 py-2 rounded-full bg-purple-600 hover:bg-purple-700 transition shadow-lg transform hover:-translate-y-0.5">
                            <span>@User.Identity.Name</span>
                        </button>

                        <div id="userDropdownMenu" class="hidden absolute right-0 mt-2 w-48 bg-gray-800 rounded-xl shadow-lg overflow-hidden z-50">
                            <a asp-controller="Home" asp-action="Conta" class="block px-4 py-2 text-gray-200 hover:bg-purple-700 hover:text-white transition">Conta</a>
                            <a asp-controller="Home" asp-action="MeusIngressos" class="block px-4 py-2 text-gray-200 hover:bg-purple-700 hover:text-white transition">Meus ingressos</a>

                            <form method="post" asp-controller="Home" asp-action="Logout">
                                <button type="submit" class="w-full text-left px-4 py-2 text-red-500 hover:bg-red-700 hover:text-white transition">
                                    Sair
                                </button>
                            </form>
                        </div>
                    </div>
                }
                else
                {
                    <div class="flex gap-3">
                        <a asp-action="Registro" class="px-4 py-2 bg-purple-600 hover:bg-purple-700 rounded-full text-white font-semibold shadow transition">Registrar</a>
                        <a asp-action="Login" class="px-4 py-2 bg-purple-600 hover:bg-purple-700 rounded-full text-white font-semibold shadow transition">Entrar</a>
                    </div>
                }
            </div>
        </div>
    </header>

    <!-- FAVORITOS -->
    <section class="pt-32 pb-24 px-6 relative z-10">
        <div class="container mx-auto">

            <div class="grid gap-12 md:grid-cols-3 items-stretch">

                @if (Model != null && Model.Any())
                {
                    @foreach (var item in Model)
                    {

                        <div class="bg-gray-800 rounded-3xl shadow-xl overflow-hidden transform transition hover:-translate-y-4 hover:scale-105 hover:shadow-2xl flex flex-col h-full min-h-[520px]">

                            <div class="relative">
                                <img src="@item.ImagemURL"
                                     alt="@item.Nome"
                                     class="w-full h-64 object-cover rounded-t-3xl">

                                <span class="absolute top-4 left-4 bg-pink-500 text-white text-sm px-3 py-1 rounded-full shadow">
                                    @item.Data.ToString("dd MMM")
                                </span>
                            </div>

                            <div class="p-6 flex flex-col flex-1 gap-4">

                                <h4 class="text-xl font-bold text-white">
                                    @item.Nome
                                </h4>

                                <p class="text-gray-300 line-clamp-3">
                                    @item.Descricao
                                </p>

                                <p class="text-pink-400 font-medium">
                                    📍 @item.Local <br>
                                    🕒 @item.Data.ToString("dd 'de' MMMM 'de' yyyy 'às' HH:mm")
                                </p>

                                <!-- BOTÕES SEMPRE ALINHADOS -->
                                <div class="flex gap-2 mt-auto">

                                    <a href="@Url.Action("Ingressos", new { id = item.Id })"
                                       class="flex-1 text-center px-4 py-2 bg-purple-600 text-white rounded-full shadow hover:bg-purple-700 transition">
                                        Comprar
                                    </a>

                                    <a asp-controller="Home"
                                       asp-action="Remover"
                                       asp-route-id="@item.Id"
                                       class="flex-1 text-center px-4 py-2 bg-red-600 text-white rounded-full shadow hover:bg-red-700 transition">
                                        Remover
                                    </a>

                                </div>

                            </div>
                        </div>
                    }
                }
                else
                {
                    <p class="text-center text-gray-400 col-span-3">
                        Nenhum evento adicionado aos favoritos.
                    </p>
                }

            </div>

        </div>
    </section>

</div>
